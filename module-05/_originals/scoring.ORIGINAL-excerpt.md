ORIGINAL trailing lines of module-04/scoring.md, BEFORE this session's edit.
The session appended a "Post-promotion changes" block after the second line below.
To revert: replace the current block with exactly these two lines.

----------------------------------------------------------------------
Promotion: candidate-a's source files (app.py, requirements.txt, README.md) copied to module-04/winner/.
Excluded from the copy: .venv/ and notes.db (environment/runtime artifacts, not deliverables).
