# Notes API

A small REST API for managing notes, backed by SQLite.

## Run

```bash
pip install -r requirements.txt
python app.py
```

The server starts on `http://localhost:8000`. The SQLite database (`notes.db`) is created automatically on first run.
